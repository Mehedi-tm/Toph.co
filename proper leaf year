#include<bits/stdc++.h>
using namespace std;
int main()
{
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    int n;
    cin>>n;
    if(n%400==0||(n%100!=0&&n%4==0))
        cout<<"Yes"<<endl;
    else
        cout<<"No"<<endl;

}
